# Módulo 10 · Integraciones — Salesforce — 12 tablas de prod (2026-08-22)

> Convención y reglas: `../../README.md`. Rarezas verificadas: `../../NOTAS-ESPEJO.md`. Veredictos de producto: `docs/v2-rediseno/04-spec-modelo-dominio-v2/00-tablas-por-modulo.md` (no aplican en este paso).
> Origen de TODO lo que está en esta carpeta: lectura en vivo de prod `hklompkypzqtglprfobu` vía MCP de Supabase el 2026-08-22 — `list_tables verbose` + `execute_sql` de solo lectura sobre `pg_catalog` (`scripts/espejo/snapshots/integraciones-salesforce.{pgmeta,catalog}.json`); metadata real de las entities existentes en `integraciones-salesforce.existing.json` (`scripts/espejo/extract-existing-metadata.ts`). Generado con `scripts/espejo/generate-espejo.py`.

## A · Tablas que YA tenían entity en el repo (11) — no se tocaron ni se duplicaron

Estas entities están **prendidas en producción** exactamente como estaban (`database.module.ts` las carga por el glob `src/**/*.entity.ts` y sus módulos las registran en `forFeature`). "Estado vs prod" = diff entre lo que la entity declara hoy (metadata TypeORM real) y la DB en vivo; es el insumo para completarlas en el paso 3 con Leon.

| Tabla (filas) | Entity existente (archivo · clase) | Estado vs prod | Columnas que faltan en la entity | Columnas que sobran | Diferencias en columnas existentes | Constraints / índices / FKs que la entity no declara |
|---|---|---|---|---|---|---|
| `salesforce_connections` (3) | `src/modules/salesforce/entities/salesforce-connection.entity.ts` · `SalesforceConnection` | ⚠️ difiere de prod | — | — | `login_url`: NOT NULL en la entity vs nullable en DB<br>`token_issued_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`is_active`: NOT NULL en la entity vs nullable en DB<br>`last_sync_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`updated_at`: NOT NULL en la entity vs nullable en DB<br>`token_expires_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`auth_type`: tipo `enum` vs DB `text`<br>`auth_type`: NOT NULL en la entity vs nullable en DB<br>`auth_type`: default `password` vs DB `'client_credentials'::text` | nombre de PK `salesforce_connections_pkey`<br>CHECK `salesforce_connections_auth_type_check`<br>FK `salesforce_connections_holding_id_fkey` → company_holdings ON DELETE CASCADE<br>FK `salesforce_connections_user_id_fkey` → users ON DELETE CASCADE<br>índice `idx_salesforce_connections_auth_type`<br>índice `idx_salesforce_connections_holding_id`<br>índice `idx_salesforce_connections_is_active`<br>índice `idx_salesforce_connections_user_id` |
| `salesforce_accounts_stg` (51) | `src/modules/salesforce/entities/salesforce-accounts-stg.entity.ts` · `SalesforceAccountsStg` | ⚠️ difiere de prod | — | — | `processed_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`last_integrated_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: NOT NULL en la entity vs nullable en DB<br>`created_at`: default `CURRENT_TIMESTAMP` vs DB `now()`<br>`updated_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`updated_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: default `CURRENT_TIMESTAMP` vs DB `now()` | nombre de PK `salesforce_accounts_stg_pkey`<br>UNIQUE `salesforce_accounts_stg_unique` (holding_id, salesforce_id)<br>CHECK `salesforce_accounts_stg_status_check`<br>índice `idx_salesforce_accounts_stg_holding_status` |
| `salesforce_opportunities_stg` (62) | `src/modules/salesforce/entities/salesforce-opportunities-stg.entity.ts` · `SalesforceOpportunitiesStg` | ⚠️ difiere de prod | — | — | `processed_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`last_integrated_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: NOT NULL en la entity vs nullable en DB<br>`created_at`: default `CURRENT_TIMESTAMP` vs DB `now()`<br>`updated_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`updated_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: default `CURRENT_TIMESTAMP` vs DB `now()` | nombre de PK `salesforce_opportunities_stg_pkey`<br>UNIQUE `salesforce_opportunities_stg_unique` (holding_id, salesforce_id)<br>CHECK `salesforce_opportunities_stg_status_check`<br>índice `idx_salesforce_opportunities_stg_account`<br>índice `idx_salesforce_opportunities_stg_holding_status` |
| `salesforce_line_items_stg` (68) | `src/modules/salesforce/entities/salesforce-line-items-stg.entity.ts` · `SalesforceLineItemsStg` | ⚠️ difiere de prod | — | — | `processed_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`last_integrated_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: NOT NULL en la entity vs nullable en DB<br>`created_at`: default `CURRENT_TIMESTAMP` vs DB `now()`<br>`updated_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`updated_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: default `CURRENT_TIMESTAMP` vs DB `now()` | nombre de PK `salesforce_line_items_stg_pkey`<br>UNIQUE `salesforce_line_items_stg_unique` (holding_id, salesforce_id)<br>CHECK `salesforce_line_items_stg_status_check`<br>FK `salesforce_line_items_stg_opportunity_staging_id_fkey` → salesforce_opportunities_stg ON DELETE SET NULL<br>índice `idx_salesforce_line_items_stg_holding_status`<br>índice `idx_salesforce_line_items_stg_opportunity`<br>índice `idx_salesforce_line_items_stg_product` |
| `salesforce_object_mappings` (1522) | `src/modules/salesforce/entities/salesforce-object-mapping.entity.ts` · `SalesforceObjectMapping` | ⚠️ difiere de prod | — | — | `last_synced_at`: default `CURRENT_TIMESTAMP` vs DB `now()` | nombre de PK `salesforce_object_mappings_pkey`<br>UNIQUE `unique_salesforce_object_per_holding` (holding_id, salesforce_object_type, salesforce_object_id)<br>FK `salesforce_object_mappings_holding_id_fkey` → company_holdings ON DELETE CASCADE<br>índice `idx_salesforce_mappings_holding`<br>índice `idx_salesforce_mappings_salesforce_lookup`<br>índice `idx_salesforce_mappings_sapira_lookup` |
| `salesforce_field_mappings` (42) | `src/modules/salesforce/entities/salesforce-field-mapping.entity.ts` · `SalesforceFieldMapping` | ⚠️ difiere de prod | — | — | `is_required`: NOT NULL en la entity vs nullable en DB<br>`is_active`: NOT NULL en la entity vs nullable en DB<br>`created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: NOT NULL en la entity vs nullable en DB | nombre de PK `salesforce_field_mappings_pkey`<br>UNIQUE `salesforce_field_mappings_holding_id_object_type_sapira_fie_key` (holding_id, object_type, sapira_field)<br>CHECK `salesforce_field_mappings_object_type_check`<br>índice `idx_salesforce_field_mappings_holding_id`<br>índice `idx_salesforce_field_mappings_object_type` |
| `salesforce_product_mappings` (109) | `src/modules/salesforce/entities/salesforce-product-mapping.entity.ts` · `SalesforceProductMapping` | ⚠️ difiere de prod | — | — | `created_at`: tipo `timestamptz` vs DB `timestamp without time zone`<br>`created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: tipo `timestamptz` vs DB `timestamp without time zone`<br>`updated_at`: NOT NULL en la entity vs nullable en DB | nombre de PK `salesforce_product_mappings_pkey`<br>FK `salesforce_product_mappings_holding_id_fkey` → company_holdings ON DELETE CASCADE<br>FK `salesforce_product_mappings_sapira_product_id_fkey` → products ON DELETE RESTRICT<br>índice `idx_sf_product_map_active` (parcial)<br>índice `idx_sf_product_map_family` (parcial)<br>índice `idx_sf_product_map_sapira_product`<br>índice `idx_sf_product_map_unique` (UNIQUE) |
| `salesforce_quote_type_mappings` (6) | `src/modules/salesforce/entities/salesforce-quote-type-mapping.entity.ts` · `SalesforceQuoteTypeMapping` | ⚠️ difiere de prod | — | — | `is_active`: NOT NULL en la entity vs nullable en DB<br>`created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: NOT NULL en la entity vs nullable en DB | nombre de PK `salesforce_quote_type_mappings_pkey`<br>UNIQUE `unique_salesforce_type_per_holding` (holding_id, salesforce_type)<br>FK `salesforce_quote_type_mappings_holding_id_fkey` → company_holdings ON DELETE CASCADE<br>índice `idx_sf_quote_type_mappings_active`<br>índice `idx_sf_quote_type_mappings_holding` |
| `salesforce_sync_runs` (20) | `src/modules/salesforce/entities/salesforce-sync-run.entity.ts` · `SalesforceSyncRun` | ⚠️ difiere de prod | — | — | `created_at`: default `CURRENT_TIMESTAMP` vs DB `now()`<br>`updated_at`: default `CURRENT_TIMESTAMP` vs DB `now()` | nombre de PK `salesforce_sync_runs_pkey`<br>CHECK `salesforce_sync_runs_status_check`<br>CHECK `salesforce_sync_runs_type_check`<br>índice `idx_salesforce_sync_runs_holding_status` |
| `salesforce_sync_run_items` (129) | `src/modules/salesforce/entities/salesforce-sync-run-item.entity.ts` · `SalesforceSyncRunItem` | ⚠️ difiere de prod | — | — | `created_at`: default `CURRENT_TIMESTAMP` vs DB `now()`<br>`updated_at`: default `CURRENT_TIMESTAMP` vs DB `now()` | nombre de PK `salesforce_sync_run_items_pkey`<br>CHECK `salesforce_sync_run_items_status_check` |
| `salesforce_opportunities_cache` (0) | `src/modules/salesforce/entities/salesforce-opportunity-cache.entity.ts` · `SalesforceOpportunityCache` | ⚠️ difiere de prod | — | — | `salesforce_id`: tipo `varchar` vs DB `text`<br>`salesforce_account_id`: tipo `varchar` vs DB `text`<br>`opportunity_name`: tipo `varchar` vs DB `text`<br>`account_name`: tipo `varchar` vs DB `text`<br>`account_name`: NOT NULL en la entity vs nullable en DB<br>`account_name`: default `Sin cuenta` vs DB `None`<br>`account_country`: tipo `varchar` vs DB `text`<br>`opportunity_type`: tipo `varchar` vs DB `text`<br>`stage_name`: tipo `varchar` vs DB `text`<br>`stage_name`: NOT NULL en la entity vs nullable en DB<br>`is_won`: NOT NULL en la entity vs nullable en DB<br>`is_closed`: NOT NULL en la entity vs nullable en DB<br>`amount`: NOT NULL en la entity vs nullable en DB<br>`amount`: default `0` vs DB `None`<br>`currency_iso_code`: tipo `varchar` vs DB `text`<br>`currency_iso_code`: NOT NULL en la entity vs nullable en DB<br>`close_date`: NOT NULL en la entity vs nullable en DB<br>`id_largo_oportunidad__c`: tipo `varchar` vs DB `text`<br>`modalidad_de_pago__c`: tipo `varchar` vs DB `text`<br>`forma_de_pago__c`: tipo `varchar` vs DB `text`<br>`contrato__c`: tipo `varchar` vs DB `text`<br>`orden_de_compra__c`: tipo `varchar` vs DB `text`<br>`quote_project_manager__c`: tipo `varchar` vs DB `text`<br>`quote_billing_email__c`: tipo `varchar` vs DB `text`<br>`line_items_count`: NOT NULL en la entity vs nullable en DB<br>`sync_date`: default `None` vs DB `CURRENT_DATE`<br>`created_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: tipo `timestamp` vs DB `timestamp with time zone`<br>`updated_at`: NOT NULL en la entity vs nullable en DB | nombre de PK `salesforce_opportunities_cache_pkey`<br>FK `salesforce_opportunities_cache_holding_id_fkey` → company_holdings ON DELETE CASCADE<br>índice `idx_sf_opp_cache_holding_id`<br>índice `idx_sf_opp_cache_salesforce_id`<br>índice `idx_sf_opp_cache_unique` (UNIQUE)<br>índice con expresión `idx_sf_opp_cache_close_date`<br>índice con expresión `idx_sf_opp_cache_sync_date` |

## B · Tablas SIN entity → espejos creados (1), APAGADOS

| Tabla (filas, RLS) | Espejo · clase | Cols | PK | UNIQUE | CHECK | FKs (→ tabla, ON DELETE) | Índices | Triggers | Policies |
|---|---|---|---|---|---|---|---|---|---|
| `salesforce_sync_logs` (0, RLS on) | `salesforce-sync-log.espejo.ts` · `SalesforceSyncLog` | 9 | `salesforce_sync_logs_pkey` (id) | — | — | `salesforce_sync_logs_holding_id_fkey` → company_holdings (CASCADE) | `idx_salesforce_sync_logs_holding_id`, `idx_salesforce_sync_logs_created_at` (expresión, no declarado), `idx_salesforce_sync_logs_sync_date` (expresión, no declarado) | — | 1 |

Cada espejo contiene, leído en vivo: columnas con tipo real (`timestamp with/without time zone`, `varchar` + `length`, `numeric` + `precision/scale`, enums de Postgres con sus valores, `text[]`, `jsonb`, `uuid`…), nullable, default y comentario; PK con nombre (`primaryKeyConstraintName`); `@Unique`/`@Check`/`@Index` con nombre real (índices parciales con `where`; los índices con expresión, orden u otro método se documentan en el JSDoc pero no se declaran porque `@Index` no los representa); una relación `@ManyToOne` por FK con `onDelete` real y `foreignKeyConstraintName` — hacia la entity existente (`@/modules/...`) si la tabla destino ya la tiene, o hacia el espejo de su módulo; cabecera JSDoc con filas, RLS, comentario de tabla, tablas que la referencian, triggers y policies (nombre, comando, roles). Las expresiones `USING`/`WITH CHECK` de las policies quedan en `scripts/espejo/snapshots/integraciones-salesforce.catalog.json` (`policies_detail`) para el paso 4.

**Cómo están apagados (código técnico)**: el archivo termina en `.espejo.ts`, no en `.entity.ts`. `database.module.ts` carga entities con `entities: [__dirname + '/../../**/*.entity{.ts,.js}']`, así que no los ve, y ningún módulo los incluye en `TypeOrmModule.forFeature([...])`. `database.module.spec.ts` falla si aparece un `.entity.ts` dentro de `entities/<modulo>/`. Para encenderlos en el paso 3: renombrar a `.entity.ts` y registrarlos en el `forFeature` del módulo que los use.

## C · Columnas exactas de cada espejo (1 tablas)

<details><summary><code>salesforce_sync_logs</code> → <code>salesforce-sync-log.espejo.ts</code> · 9 columnas</summary>

| Columna | Tipo Postgres | Nulo | Default | Comentario |
|---|---|---|---|---|
| `id` 🔑 | uuid | no | gen_random_uuid() |  |
| `holding_id` | uuid | sí | — |  |
| `sync_date` | date | no | CURRENT_DATE | Fecha de los datos sincronizados (no la fecha de ejecución) |
| `opportunities_count` | integer | sí | 0 | Número de oportunidades encontradas en la sincronización |
| `accounts_count` | integer | sí | 0 |  |
| `success` | boolean | sí | false |  |
| `error_message` | text | sí | — |  |
| `execution_time_ms` | integer | sí | — | Tiempo de ejecución en milisegundos |
| `created_at` | timestamp with time zone | sí | now() |  |

</details>

## Verificación (sin conexión a la DB)

- `integraciones-salesforce.entities.spec.ts`: metadata TypeORM en memoria vs `integraciones-salesforce.prod-snapshot.ts` — columnas + nullabilidad, PK, FKs (tabla y ON DELETE), UNIQUE, CHECK e índices declarables — y que ningún espejo duplica una tabla de `scripts/espejo/existing-entities.json`.
- `../../../database.module.spec.ts`: `synchronize: false`, nadie habilita sincronización, ningún `.entity.ts` dentro de `entities/<modulo>/`.
