# Módulo 10 · Integraciones — Stripe — 7 tablas de prod (2026-08-22)

> Convención y reglas: `../../README.md`. Rarezas verificadas: `../../NOTAS-ESPEJO.md`. Veredictos de producto: `docs/v2-rediseno/04-spec-modelo-dominio-v2/00-tablas-por-modulo.md` (no aplican en este paso).
> Origen de TODO lo que está en esta carpeta: lectura en vivo de prod `hklompkypzqtglprfobu` vía MCP de Supabase el 2026-08-22 — `list_tables verbose` + `execute_sql` de solo lectura sobre `pg_catalog` (`scripts/espejo/snapshots/integraciones-stripe.{pgmeta,catalog}.json`); metadata real de las entities existentes en `integraciones-stripe.existing.json` (`scripts/espejo/extract-existing-metadata.ts`). Generado con `scripts/espejo/generate-espejo.py`.

## A · Tablas que YA tenían entity en el repo (7) — no se tocaron ni se duplicaron

Estas entities están **prendidas en producción** exactamente como estaban (`database.module.ts` las carga por el glob `src/**/*.entity.ts` y sus módulos las registran en `forFeature`). "Estado vs prod" = diff entre lo que la entity declara hoy (metadata TypeORM real) y la DB en vivo; es el insumo para completarlas en el paso 3 con Leon.

| Tabla (filas) | Entity existente (archivo · clase) | Estado vs prod | Columnas que faltan en la entity | Columnas que sobran | Diferencias en columnas existentes | Constraints / índices / FKs que la entity no declara |
|---|---|---|---|---|---|---|
| `stripe_connections` (1) | `src/modules/stripe/entities/stripe-connection.entity.ts` · `StripeConnection` | ⚠️ difiere de prod | — | — | `created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: NOT NULL en la entity vs nullable en DB | nombre de PK `stripe_connections_pkey`<br>CHECK `stripe_connections_mode_check`<br>índice `idx_stripe_connections_holding_id`<br>índice `idx_stripe_connections_is_active`<br>índice `idx_stripe_connections_user_id` |
| `stripe_customers_stg` (407) | `src/modules/stripe/entities/stripe-customers-stg.entity.ts` · `StripeCustomersStg` | ⚠️ difiere de prod | — | — | `created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: NOT NULL en la entity vs nullable en DB | nombre de PK `stripe_customers_stg_pkey`<br>UNIQUE `unique_stripe_customer_per_holding` (holding_id, stripe_id)<br>índice `idx_stripe_customers_stg_connection_id`<br>índice `idx_stripe_customers_stg_holding_id`<br>índice `idx_stripe_customers_stg_processing_status`<br>índice `idx_stripe_customers_stg_stripe_id`<br>índice `idx_stripe_customers_stg_sync_batch_id` |
| `stripe_invoices_stg` (3795) | `src/modules/stripe/entities/stripe-invoices-stg.entity.ts` · `StripeInvoicesStg` | ⚠️ difiere de prod | — | — | `created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: NOT NULL en la entity vs nullable en DB | nombre de PK `stripe_invoices_stg_pkey`<br>UNIQUE `unique_stripe_invoice_per_holding` (holding_id, stripe_id)<br>índice `idx_stripe_invoices_stg_connection_id`<br>índice `idx_stripe_invoices_stg_holding_id`<br>índice `idx_stripe_invoices_stg_processing_status`<br>índice `idx_stripe_invoices_stg_stripe_id`<br>índice `idx_stripe_invoices_stg_sync_batch_id` |
| `stripe_subscriptions_stg` (533) | `src/modules/stripe/entities/stripe-subscriptions-stg.entity.ts` · `StripeSubscriptionsStg` | ⚠️ difiere de prod | — | — | `created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: NOT NULL en la entity vs nullable en DB | nombre de PK `stripe_subscriptions_stg_pkey`<br>UNIQUE `unique_stripe_subscription_per_holding` (holding_id, stripe_id)<br>índice `idx_stripe_subscriptions_stg_connection_id`<br>índice `idx_stripe_subscriptions_stg_holding_id`<br>índice `idx_stripe_subscriptions_stg_processing_status`<br>índice `idx_stripe_subscriptions_stg_stripe_id`<br>índice `idx_stripe_subscriptions_stg_sync_batch_id` |
| `stripe_product_mappings` (75) | `src/modules/stripe/entities/stripe-product-mapping.entity.ts` · `StripeProductMapping` | ⚠️ difiere de prod | — | — | `created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: NOT NULL en la entity vs nullable en DB<br>`metadata`: NOT NULL en la entity vs nullable en DB | nombre de PK `stripe_product_mappings_pkey`<br>UNIQUE `unique_stripe_mapping` (holding_id, sapira_product_id, stripe_product_id)<br>FK `stripe_product_mappings_holding_id_fkey` → company_holdings ON DELETE CASCADE<br>índice `idx_stripe_mappings_holding`<br>índice `idx_stripe_mappings_holding_stripe`<br>índice `idx_stripe_mappings_sapira_product`<br>índice `idx_stripe_mappings_stripe_product` |
| `stripe_sync_jobs` (177) | `src/modules/stripe/entities/stripe-sync-job.entity.ts` · `StripeSyncJob` | ⚠️ difiere de prod | — | — | — | nombre de PK `stripe_sync_jobs_pkey`<br>índice `idx_stripe_sync_jobs_holding_id`<br>índice `idx_stripe_sync_jobs_status`<br>índice con expresión `idx_stripe_sync_jobs_created_at` |
| `stripe_customers_bigquery` (26351) | `src/databases/postgresql/entities/stripe-customer-bigquery.entity.ts` · `StripeCustomerBigQuery` | ⚠️ difiere de prod | — | — | `created_at`: NOT NULL en la entity vs nullable en DB<br>`updated_at`: default `None` vs DB `now()` | nombre de PK `stripe_customers_bigquery_pkey`<br>FK `stripe_customers_bigquery_holding_id_fkey` → company_holdings ON DELETE CASCADE<br>índice `idx_stripe_customers_bigquery_holding_id`<br>índice `idx_stripe_customers_bigquery_salesforce_account_id`<br>índice `idx_stripe_customers_bigquery_stripe_customer_id` |

## B · Tablas SIN entity → espejos creados (0), APAGADOS

Ninguna: todas las tablas de este módulo ya tienen entity en el repo; este módulo solo documenta el diff (sección A). No hay espejos, snapshot ni spec.
